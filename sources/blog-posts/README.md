# Drafted sources for the recent notes

The markdown originals the 2026 notes were written from, with the Typst
sources, data and scripts for their figures. Inputs to the migration, kept
because they carry authoring intent a Ghost HTML render cannot. See
`sources/README.md`.

**The writing plan that used to live here has moved to `WRITING-PLAN.md` at the
repository root.** It is a live planning document and this directory is
described, correctly, as migration input that nobody should be editing.
